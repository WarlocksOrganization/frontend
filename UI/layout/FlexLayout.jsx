'use client'
import FlexContainer from "../../styles/componenets/FlexContainer";

const FlexLayout = ({children}) => {

    return (
        <FlexContainer>
            {children}
        </FlexContainer>
    )
 }

 export default FlexLayout